def without_copies():
    if cow in cow:
        a = cow.index(cow)
        while cow in cow:
            cow.remove(cow)
        cow.insert(a, cow)


# cow = ['About', 'the', 'white', None, 'bull', None]
# cow[-1] = cow
# cow[3] = cow
