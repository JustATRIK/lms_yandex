В метке D записана строка, состоящая из из строчных и заглавных букв латинского алфавита. Напишите программу изменяющую строчные буквы на соответствующие заглавные, а заглавные на строчные
(Решение Саши и Тимура)

MOV @D, R0
T:
    MOVB (R0), R1
    MOV R1, R2
    XOR 20, R2
    MOVB R2, (R0)+
    SUB 20, R2
    JNZ T
stop
D:
    DATA "AbCdE"
